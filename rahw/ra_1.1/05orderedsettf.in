%DESC True/false questions on ordered sets
%HINC head.txt
%NOPARTIAL
True or false:
If \(S\) is a nonempty finite ordered set, then \(\sup S\) exists and \(\sup S \in S.\)
$BR
%RADIO
%CORRECT True
False
%END

True or false:
If \(S\) is an ordered set and \(A \subset S\) is a bounded nonempty subset, then the least upper bound of \(A\) exists.
$BR
%RADIO
True
%CORRECT False
%END

True or false:
The set of rational numbers has the least-upper-bound property.
$BR
%RADIO
True
%CORRECT False
%END

True or false:
In any ordered field for any nonzero \(x,y,z,\) we have \(x^2+y^2+z^2 $GTS 0. \)
$BR
%RADIO
%CORRECT True
False
%END

True or false:
Every field can be ordered, that is, made into an ordered field by defining an order relation.
$BR
%RADIO
True
%CORRECT False
%END
