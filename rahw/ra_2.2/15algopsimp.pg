##DESCRIPTION
## Compute and prove a simple limit by using algebraic operations
##ENDDESCRIPTION

## DBsubject(Real analysis)
## DBchapter()
## DBsection()
## Institution(Oklahoma State University)
## Author(Jiri Lebl)
## Level(6)
## TitleText1('Basic Analysis I: Introduction to Real Analysis, Volume I')
## AuthorText1('Jiri Lebl')
## EditionText1('5')
## Section1('2.2')
## Problem1('')

######################################################

DOCUMENT();

loadMacros(
 "PGstandard.pl",
 "MathObjects.pl",
 "AnswerFormatHelp.pl",
 "draggableProof.pl",
 "PGcourse.pl",
);

TEXT(beginproblem());

###########################
# Setup

$showPartialCorrectAnswers = 0;

Context("Numeric");


$a=random(2,9,1);

############################
# Main text

TEXT(MODES(HTML => '<span style="font-size:120%;">'));
Context()->texStrings;
BEGIN_TEXT
$PAR
Prove that \({\left\lbrace \frac{$a n^2-n}{n^2+1} \right\rbrace}_{n=1}^\infty\) converges to
END_TEXT
Context()->normalStrings;

$q1 = Formula("$a");

Context()->texStrings;
BEGIN_TEXT
\{ ans_rule(20) \}
\{ AnswerFormatHelp("numbers") \}
$PAR
END_TEXT
Context()->normalStrings;

$q2 = DraggableProof([
"Write \(\frac{$a n^2-n}{n^2+1} = \frac{$a-\frac{1}{n}}{1+\left(\frac{1}{n}\right)\left(\frac{1}{n}\right)}\text{.}\)",
"As \(\lim\limits_{n\to\infty} \frac{1}{n} = 0 \text{,}\) we have \(\lim\limits_{n\to\infty} \left($a-\frac{1}{n}\right) = $a\) and \(\lim\limits_{n\to\infty} \left(1+\left(\frac{1}{n}\right)^2\right) = 1\text{.}\)",
"So as the denominator does not go to zero, \(\lim\limits_{n\to\infty} \frac{$a-\frac{1}{n}}{1+\left(\frac{1}{n}\right)^2} = \frac{\lim_{n\to\infty} $a-\frac{1}{n}}{\lim_{n\to\infty} 1+\left(\frac{1}{n}\right)^2} = $a\text{.}\)",
],
[
"Write \(\frac{$a n^2-n}{n^2+1} $LTS \frac{$a-\frac{1}{n}}{1+\left(\frac{1}{n}\right)\left(\frac{1}{n}\right)}\text{.}\)",
"\(\left\lvert\frac{$a n^2-n}{n^2+1}\right\rvert $LTS 1\text{.}\)",
"Let \(\epsilon:=1\text{.}\)",
"As \(\lim\limits_{n\to\infty} \frac{1}{n} = 0\) and the denominator does not go to zero, \(\lim\limits_{n\to\infty} \frac{$a n^2-n}{n^2+1} = 1\text{.}\)",
],

SourceLabel => "Choose from these statements",
TargetLabel =>  "The proof",
);

Context()->texStrings;
BEGIN_TEXT

$BITALIC (Drag the statements from the first column
to the second column, putting them in the correct order.  There could be extras.) $EITALIC

\{ $q2->Print \}


END_TEXT
Context()->normalStrings;

############################
# Answers

install_problem_grader(~~&std_problem_grader);

ANS($q1->cmp());
ANS($q2->cmp());

TEXT(MODES(HTML => '</span>'));

COMMENT("Compute and prove a simple limit by using algebraic operations");

ENDDOCUMENT();
